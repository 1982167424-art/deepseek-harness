/**
 * Browser entry: registers the invariant companion. The real client surface
 * (`ReasoningEffortSelect` + slot occupant) lives under `./client/` and is
 * emitted as the `./client` conditional export by tsdown.
 *
 * @module @deepseek-ai/dsh-client-ui-reasoning-effort
 */
export * from './invariant.ts'
