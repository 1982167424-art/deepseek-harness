/**
 * Task board UI plugin, Host half: a Loader stub so the browser half can be
 * discovered and mounted by the web roster.
 * @module @deepseek-ai/dsh-client-ui-task-board
 */

/** Host plugin body; every behavior lives in the `./client` browser half. */
export function apply(): void {}
