/** Runtime-shared invariants package re-export stub.
 *
 * Copy of the ui-sidebar pattern: the real invariant lives in
 * @deepseek-ai/dsh-invariants; this module is a stable re-export face so the
 * package's own exports don't widen when the shared package adds checks.
 */
